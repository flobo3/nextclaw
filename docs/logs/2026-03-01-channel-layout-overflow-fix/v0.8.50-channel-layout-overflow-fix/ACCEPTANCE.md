# Acceptance

## 用户 / 产品视角验收步骤

1. 打开 UI 的 `Channels` 页面，选择配置项较多的渠道（如飞书）。
2. 持续向下滚动配置项，确认滚动发生在右侧表单卡片内部。
3. 观察页面整体：不应再出现“整页可继续下滚的大块空白区域”。
4. 切换到 `Providers` 页面，对比布局与滚动行为，应保持同构一致。
5. 检查图标与卡片轮廓尺寸，渠道页与提供商页应一致。
