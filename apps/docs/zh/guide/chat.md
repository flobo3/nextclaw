# 对话能力

当前对话能力（面向用户）：

- 多轮会话：按会话持续上下文，不用每轮重述背景。
- 模型切换：可按会话指定模型，便于在速度和效果间切换。
- 技能加成：发送前可选择已安装 `skills`，让本轮回答遵循特定规则。
- 工具调用：可在对话中调用已启用工具（如搜索、命令等）。
- 渠道一致：同一套核心能力可复用到本地 UI 与已接入渠道。
- 会话可管理：支持搜索、查看历史、清空、删除与重命名。

如果你想直接上手技能，见：[Skills 教程](/zh/guide/tutorials/skills)。
